#include <stdio.h>
int main() {
  char *p = "Godwin";
  printf("%c", *(p + 0));
  for (int i = 0; i < 5; i++) {
    printf("HI");
  }
  // printf("%d",p[1]-p[0]))

  // int x = 5;

  // int y = 7;
  // int arr[] = {1,2,3,4};
  // int *p = &y;

  // int *q = &x;

  // printf("%d",(char*)p-(char*)q);

  // printf("%.2f",7.0);

  // distance * sizeof(int) / sizeof(char)
}